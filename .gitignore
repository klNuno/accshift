# Dependencies
node_modules/

# Build outputs
dist/
target/
scripts/social-card.html
src-tauri/keys/
src-tauri/binaries/
# Regenerated by tauri-build on every build, and the contents depend on
# which features are on, so a plain `cargo clippy` rewrites what a
# `--features mcp-bridge` build wrote. Nothing reads them but the IDE.
src-tauri/gen/schemas/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Logs
*.log
npm-debug.log*
